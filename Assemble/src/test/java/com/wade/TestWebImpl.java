package com.wade.service;
import junit.framework.Assert;
import org.junit.Test;

public class TestWebImpl{
    @Test
    public void testCreateUser(){
        // Assert.assertEquals(App.HelloWorld(), "HelloWorld");
      Assert.assertEquals(0 , 0);
    }
}
